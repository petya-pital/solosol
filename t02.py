import sys # подгрузили библеотеку sys
#print(' current version :' + str(sys.version) + "\nits info\n" + sys.version_info.__repr__())
print(' current version :' + str(sys.version) + "\nits info\n" + str(sys.version_info))
print(sys.version_info)